# rename this file to SMMID.yml and put a : in front of "name" if
# you want to use yaml like in old versions of Catalyst
name SMMID

access_log __path_to(logs/smmid-access.log)__
error_log  __path_to(logs/smmid-error.log)__
